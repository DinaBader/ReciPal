import { StyleSheet } from "react-native";

const style=StyleSheet.create({
    circle: {
        width: 20,
        height: 20,
        borderRadius: 50,
        marginBottom:5,
        marginLeft:28,
        marginTop:10,
      },
      align:{
        display:'flex',
        flexDirection:'row',
        gap:2,
        alignItems:'center'
      }
})

export default style