import { StyleSheet } from "react-native";

const styles= StyleSheet.create({
    overlayText: {
        position: 'absolute',
        bottom: 10, 
        left: 30, 
        color: 'white', 
        fontSize: 30, 
        fontWeight: 'bold', 
      },
})

export default styles;