import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    text:{
        color:'#FFFFFF',
        textAlign:'center',
        marginTop:10,
        fontWeight:'bold'
    }
})

export default styles;